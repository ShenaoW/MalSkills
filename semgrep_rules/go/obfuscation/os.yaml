rules:
- id: sso.go.obfuscation.os.0001.no-os.Open
  languages:
  - go
  message: Avoid using os.Open(), as it can lead to potential issues with obfuscation.
  metadata:
    api: os.Open()
    category: Obfuscation
    malskills_sso_category: cryptography
    malskills_subtype: encoding
  pattern-either:
  - pattern: os.Open(...)
  - pattern: $VAR = os.Open
  severity: WARNING
  tags:
  - Obfuscation
- id: sso.go.obfuscation.os.0002.no-os.ReadFile
  languages:
  - go
  message: Avoid using os.ReadFile(), as it can lead to potential issues with obfuscation.
  metadata:
    api: os.ReadFile()
    category: Obfuscation
    malskills_sso_category: cryptography
    malskills_subtype: encoding
  pattern-either:
  - pattern: os.ReadFile(...)
  - pattern: $VAR = os.ReadFile
  severity: WARNING
  tags:
  - Obfuscation

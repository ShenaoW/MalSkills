rules:
- id: no-hmac.Equal
  languages:
  - go
  message: Avoid using hmac.Equal(), as it can lead to potential issues with cryptography.
  metadata:
    api: hmac.Equal()
    category: Cryptography
  pattern-either:
  - pattern: hmac.Equal(...)
  - pattern: $VAR = hmac.Equal
  severity: WARNING
  tags:
  - Cryptography
- id: no-hmac.New
  languages:
  - go
  message: Avoid using hmac.New(), as it can lead to potential issues with cryptography.
  metadata:
    api: hmac.New()
    category: Cryptography
  pattern-either:
  - pattern: hmac.New(...)
  - pattern: $VAR = hmac.New
  severity: WARNING
  tags:
  - Cryptography
